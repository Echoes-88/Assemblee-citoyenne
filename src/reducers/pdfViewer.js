export const initialState = {
    pdfFile: "/pdf/test.pdf"
};

const pdfViewer = (state = initialState, action = {}) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default pdfViewer;
