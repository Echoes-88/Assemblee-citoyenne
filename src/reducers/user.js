export const initialState = {
    isLogged: false,
};

const User = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default User;